version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build started on `date`
      - ./gradlew build  # Use the Gradle wrapper to build your project
  post_build:
    commands:
      - echo Build completed on `date`
      - zip -r Lambda.zip build/*  # Package the build artifacts into a zip file

artifacts:
  files:
    - Lambda.zip